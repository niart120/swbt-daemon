# Work In Progress Specs

Create active implementation work units as:

```text
spec/wip/local_{nnn}/FEATURE_NAME.md
```

Use uppercase snake case for `FEATURE_NAME.md`. Move completed work units to `spec/complete/local_{nnn}/` only after requirements, source audit, tests, hardware status, and checklist evidence are updated.
