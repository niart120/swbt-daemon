# spec (WIP)

作業中のspecは次の形式で作る。

```text
spec/wip/local_{nnn}/FEATURE_NAME.md
```

`FEATURE_NAME.md` は UPPER_SNAKE_CASE にする。
要件、根拠監査、テスト、実機状態、チェックリストの根拠を更新してからだけ、完了したspecを `spec/complete/local_{nnn}/` に移す。
